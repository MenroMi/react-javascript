import React from "react";
import ReactDOM from "react-dom/client";
import ComponentBlock from "./App";


const root = ReactDOM.createRoot(document.querySelector('#root'));
root.render(
    <div>
        <ComponentBlock/>
    </div>
);